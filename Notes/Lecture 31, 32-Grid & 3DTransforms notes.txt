grid-template-columns: repeat(auto-fit, minmax(200px, 500px));
This property will help us to create responsive columns, we added a property that every column must be a minimum of 200px and a maximum of 500px, which means if we try to resize, then it will responsively update the number of columns in that grid.

This will help us to make the site responsive without the use of media queries, for example in the Blog project, instead of using the media queries, we could have added 2 loc for a better result:

.main_container{
    display: grid;
    gap: 1.5rem;
    /* grid-template-columns: repeat(1, 1fr);
    grid-template-rows: repeat(2, 1fr); */

    grid-template-columns: repeat(auto-fit, minmax(350px, 400px));
    justify-content: center;
}

/* @media (min-width: 650px) {
    .main_container{
    grid-template-columns: repeat(2, 1fr);
    }
}

@media (min-width: 1080px) {
    .main_container{
    grid-template-columns: repeat(3, 1fr);
    }
}

@media (min-width: 1340px) {
    .main_container{
    grid-template-columns: repeat(4, 1fr);
    }
} */

we made a complex layout using grids but it is easier if we can create a 2D matrix and then start naming the cells.

            grid-template-areas: 
                "header header header"
                "sidebar content1 content1"
                "sidebar content2 content3"
                "footer footer footer"
            ;
This is the easiest way to create grid template ares.

We must use ids and then use the ids to replicate the structure. here header is the id of the header div tag.


3D TRANSFORM NOTES:

Now we will learn about the properties like translation, scaling, rotation etc.

    #rotatex:hover{
        transform: rotateX(45deg);
    }

    #rotatey:hover{
        transform: rotateY(45deg);
    }

    #rotatez:hover{
        transform: rotateZ(45deg);
    }

we can use these properties as is for X,Y and Z axes but we will need to set a perspective for the Z axis for Scale and Translate property.

Here is the Translate Property:

    #translatex:hover{
        transform: translateX(100px);
    }

    #translatey:hover{
        transform: translateY(100px);
    }

    #translatez:hover{
        transform:perspective(300px) translateZ(100px);
    }

Here is the Scale Property:

    #scalex:hover{
        transform: scaleX(1.5);
    }

    #scaley:hover{
        transform: scaleY(1.5);
    }

    #scalez:hover{
        transform:perspective(300px) scale(3) rotateY(30deg);
    }